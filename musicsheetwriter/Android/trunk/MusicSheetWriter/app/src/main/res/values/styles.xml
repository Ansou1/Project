<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/accent</item>
    </style>


    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="AppTheme.TabTextApearance" parent="TextAppearance.AppCompat" />

    <style name="AppTheme.FloatingButtonMenu">
        <item name="android:background">@drawable/fab_label_background</item>
        <item name="android:textColor">@color/text_icons_over_primary</item>
    </style>

    <!-- Base application light theme for search activity. -->
    <style name="AppTheme.Light" parent="Theme.AppCompat.Light" />
    <style name="AppTheme.Light.NoActionBar" parent="Theme.AppCompat.Light.NoActionBar" />

    <style name="AppTheme.TextInputLayoutOverPrimary" parent="TextAppearance.AppCompat">
        <!-- Hint color and label color in FALSE state -->
        <item name="android:textColorHint">@color/text_icons_over_primary</item>
        <item name="android:textSize">20sp</item>
        <!-- Label color in TRUE state and bar color FALSE and TRUE State -->
        <item name="colorAccent">@color/accent</item>
        <item name="colorControlNormal">@color/text_icons_over_primary</item>
        <item name="colorControlActivated">@color/accent</item>
    </style>

</resources>
